apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    feature-gate.release.openshift.io/: "true"
  name: ignitionpayloads.hypershift.openshift.io
spec:
  group: hypershift.openshift.io
  names:
    kind: IgnitionPayload
    listKind: IgnitionPayloadList
    plural: ignitionpayloads
    shortNames:
    - ignpayload
    singular: ignitionpayload
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.current.generation
      name: Generation
      type: integer
    - jsonPath: .status.conditions[?(@.type=="PayloadGenerated")].status
      name: Generated
      type: string
    - jsonPath: .status.conditions[?(@.type=="IgnitionReached")].status
      name: Reached
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          IgnitionPayload is one consumer's request for an ignition payload. Consumers own
          spec; the payload controller owns Current, Previous, and PayloadGenerated; the
          serving tier may only set IgnitionReached through a field-scoped status patch.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec is written by a consumer such as the NodePool or Karpenter
              controller.
            properties:
              additionalTrustBundle:
                description: additionalTrustBundle optionally identifies the CA bundle
                  ConfigMap.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              mgmtConfig:
                description: |-
                  mgmtConfig are management-side-only ConfigMaps. Changes refresh the
                  bytes behind the current token without advancing rollout generation.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              osStream:
                description: osStream selects the RHEL stream used for generated payloads.
                maxLength: 253
                minLength: 1
                type: string
              pullSecretName:
                description: pullSecretName names the Secret in this namespace used
                  to pull ReleaseImage.
                maxLength: 253
                minLength: 1
                type: string
              releaseImage:
                description: releaseImage is the OCP release image providing machine-config
                  binaries.
                maxLength: 2048
                minLength: 1
                type: string
              rendererInputs:
                description: |-
                  rendererInputs contains renderer-only data that is deliberately distinct
                  from rollout inputs. It is written by the consumer and read by the payload controller.
                properties:
                  cloudConfig:
                    description: |-
                      cloudConfig identifies the Azure or OpenStack cloud configuration ConfigMap.
                      It is absent for platforms without cloud configuration input.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  cloudConfigHash:
                    description: cloudConfigHash is HashConfigMapData over CloudConfigRef
                      and prevents stale cloud config from rendering.
                    maxLength: 253
                    minLength: 1
                    type: string
                  machineConfigServerConfig:
                    description: |-
                      machineConfigServerConfig identifies the HCP-owned machine-config-server ConfigMap.
                      The payload controller verifies its configuration-hash before rendering.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  machineConfigServerConfigHash:
                    description: machineConfigServerConfigHash is the backward-compatible
                      HostedCluster configuration hash.
                    maxLength: 253
                    minLength: 1
                    type: string
                  managementGlobalConfig:
                    description: |-
                      managementGlobalConfig is canonical full global configuration. It changes
                      payload identity but not rollout identity, so replacement nodes get current
                      content while existing nodes remain untouched.
                    maxLength: 1048576
                    minLength: 1
                    type: string
                required:
                - machineConfigServerConfig
                - machineConfigServerConfigHash
                type: object
                x-kubernetes-validations:
                - message: machineConfigServerConfig.name must be machine-config-server
                  rule: self.machineConfigServerConfig.name == 'machine-config-server'
                - message: cloudConfig and cloudConfigHash must be specified together
                  rule: has(self.cloudConfig) == has(self.cloudConfigHash) && (!has(self.cloudConfig)
                    || (self.cloudConfig.name.size() > 0 && self.cloudConfigHash.size()
                    > 0))
                - message: cloudConfig.name must identify a supported platform cloud
                    config
                  rule: '!has(self.cloudConfig) || self.cloudConfig.name in [''azure-cloud-config'',
                    ''openstack-cloud-config'']'
              retiredGeneration:
                description: |-
                  retiredGeneration tells the payload controller that all nodes using this
                  generation have drained and the previous token may be deleted.
                format: int64
                minimum: 0
                type: integer
              rolloutConfig:
                description: rolloutConfig are ConfigMaps whose changes require a
                  new rollout generation.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              rolloutGlobalConfig:
                description: rolloutGlobalConfig is the canonical subset whose change
                  requires nodes to roll.
                maxLength: 1048576
                minLength: 1
                type: string
            required:
            - pullSecretName
            - releaseImage
            - rendererInputs
            type: object
            x-kubernetes-validations:
            - message: pullSecretName must be a valid DNS subdomain name
              rule: self.pullSecretName.matches('^[a-z0-9]([-a-z0-9]*[a-z0-9])?([.][a-z0-9]([-a-z0-9]*[a-z0-9])?)*$')
            - message: additionalTrustBundle.name must not be empty
              rule: '!has(self.additionalTrustBundle) || self.additionalTrustBundle.name.size()
                > 0'
            - message: mgmtConfig names must not be empty
              rule: '!has(self.mgmtConfig) || self.mgmtConfig.all(ref, ref.name.size()
                > 0)'
            - message: rolloutConfig names must not be empty
              rule: '!has(self.rolloutConfig) || self.rolloutConfig.all(ref, ref.name.size()
                > 0)'
          status:
            description: status is written by the payload controller and the serving
              tier as documented above.
            minProperties: 1
            properties:
              conditions:
                description: |-
                  conditions is a map keyed by type. PayloadGenerated is controller-owned and
                  IgnitionReached is written by the serving tier without replacing other status fields.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              current:
                description: current is owned by the payload controller and identifies
                  the newest generated payload.
                properties:
                  configHash:
                    description: configHash identifies the complete validated renderer
                      input.
                    maxLength: 253
                    minLength: 1
                    type: string
                  generation:
                    description: generation advances only when RolloutHash changes.
                    format: int64
                    minimum: 0
                    type: integer
                  rolloutHash:
                    description: rolloutHash identifies the rollout-relevant subset
                      of input.
                    maxLength: 253
                    minLength: 1
                    type: string
                  token:
                    description: token is the opaque store key served to booting nodes.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - configHash
                - generation
                - rolloutHash
                - token
                type: object
              previous:
                description: previous is owned by the payload controller and remains
                  servable while a rollout drains.
                properties:
                  configHash:
                    description: configHash identifies the complete validated renderer
                      input.
                    maxLength: 253
                    minLength: 1
                    type: string
                  generation:
                    description: generation advances only when RolloutHash changes.
                    format: int64
                    minimum: 0
                    type: integer
                  rolloutHash:
                    description: rolloutHash identifies the rollout-relevant subset
                      of input.
                    maxLength: 253
                    minLength: 1
                    type: string
                  token:
                    description: token is the opaque store key served to booting nodes.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - configHash
                - generation
                - rolloutHash
                - token
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
